project CodePeer extends "tictactoe.gpr" is

   for Target use "codepeer";

   for Object_Dir use "."; -- uses the ./codepeer/ directory

   package Reporter is
      for Default_Switches ("Ada") use ("--show=rank-high");
   end Reporter;

end CodePeer;
