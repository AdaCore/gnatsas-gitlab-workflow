project GNATSAS extends "tictactoe.gpr" is

   for Target use "codepeer";

   for Object_Dir use "."; -- uses the ./gnatsas/ directory

   package Reporter is
      for Default_Switches ("Ada") use ();
   end Reporter;

end GNATSAS;
